<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.paulosales.schedule">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".application.ScheduleApp">
        <activity
            android:name=".ui.splashscreen.SplashActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:screenOrientation="portrait"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.dashboard.DashBoardActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.schedule.ScheduleActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.editschedule.EditScheduleActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.addschedule.AddScheduleActivity"
            android:screenOrientation="portrait" />
    </application>
</manifest>